#include "include/header.h"

/**
 * @brief This is an example of a function that needs to be linked to the main.c.
 * 
 * @param a int
 * @param b int
 * @return int a + b
 */
int hFunc(int a, int b) {
    return a + b;
}